def get_prime_numbers(number):
	prime = 0;
	primeNumbers = []
	for index in range(2, number):
		for count in range(2, index):
		counter = 2;
		if index % count == 0:
			return index


def get_number_primed(integer):
	
		if index % count == 0:


		
    