def get_vowel(word : letter)
    vowel = "A E I O U"
    count = 0
    for vowel in vowels:
        for character in word:
            if character.upper() = vowel
                count += 1
    return count

userInput = input("Enter a string: ")
print(get_vowel(word : letter)
